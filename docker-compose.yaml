# More info at https://github.com/pi-hole/docker-pi-hole/ and https://docs.pi-hole.net/
services:
  dnscrypt-proxy:
    image: klutchell/dnscrypt-proxy:latest
    container_name: dnscrypt-proxy
    networks:
      - sozuholenet
    ports:
      - "5053:5053/udp"
      - "5053:5053/tcp"

    volumes:
      - './dnsproxyconfig:/config:rw'

    healthcheck:
      test:
        [
          "CMD",
          "/usr/local/bin/dnsprobe",
          "google.com",
          "127.0.0.1:5053"
        ]

      # Equivalent-ish to the Kubernetes probes
      interval: 120s
      timeout: 3s
      retries: 3
      start_period: 30s
    restart: unless-stopped

  pihole:
    container_name: pihole
    hostname: pihole-docker
    image: pihole/pihole:latest
    depends_on:
      - dnscrypt-proxy
    networks:
      - sozuholenet
    ports:
      # DNS Ports - mapped directly to avoid complications with in-container DNS lookups failing due to
      # docker networking DNS resolution interfering.
      - "${HOST_IP}:53:53/tcp"
      - "${HOST_IP}:53:53/udp"
      
      # Default HTTP Port
      - "8100:80/tcp"
      # Default HTTPs Port. FTL will generate a self-signed certificate
      - "4433:443/tcp"
      
      # Uncomment the below if using Pi-hole as your DHCP Server
      #- "67:67/udp"
      
      # Uncomment the line below if you are using Pi-hole as your NTP server
      #- "123:123/udp"
    
    environment:
      # Set the appropriate timezone for your location from
      # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones, e.g:
      TZ: 'Europe/London'
      # Set a password to access the web interface. Not setting one will result in a random password being assigned
      #FTLCONF_webserver_api_password: 'bingo mash phillip coronet'
      # If using Docker's default `bridge` network setting the dns listening mode should be set to 'ALL'
      FTLCONF_dns_listeningMode: 'ALL'
    
    # Volumes store your data between container upgrades
    volumes:
      # For persisting Pi-hole's databases and common configuration file
      - './etc-pihole:/etc/pihole'
      - './logs:/var/log/pihole'

      # Uncomment the below if you have custom dnsmasq config files that you want to persist. Not needed for most starting fresh with Pi-hole v6. If you're upgrading from v5 you and have used this directory before, you should keep it enabled for the first v6 container start to allow for a complete migration. It can be removed afterwards. Needs environment variable FTLCONF_misc_etc_dnsmasq_d: 'true'
      #- './etc-dnsmasq.d:/etc/dnsmasq.d'

    cap_add:
      # See https://docs.pi-hole.net/docker/configuration/#note-on-capabilities
      # Required if you are using Pi-hole as your DHCP server, else not needed
      #- NET_ADMIN
      # Required if you are using Pi-hole as your NTP client to be able to set the host's system time
      #- SYS_TIME
      # Optional, if Pi-hole should get some more processing time
      - SYS_NICE

    restart: unless-stopped

networks:
  sozuholenet:
    driver: bridge
